# 儿童奖惩转盘产品需求文档（PRD）

> **【版本更新说明（V3 架构升级与自适应优化）】**
*   **架构重构**：打破固定的“奖励/惩罚”双模式限制，采用全新的“主页列表 ➔ 游玩 ➔ 编辑”三层架构，支持用户无限创建、命名和管理完全自定义的专属转盘。
*   **字体自适应缩放**：新增文本动态渲染算法。系统会根据转盘的总选项数量（超过6个时）以及单项文本长度（超过4字时）自动缩小对应文字的字号，并在极限情况下提供安全下限，彻底解决多选项及长文本情况下的视觉重叠和溢出问题。

**【版本更新说明（V2 双端同步与功能增强）】**
> 本次文档针对实际开发过程中的需求变更进行了迭代，核心更新包括：
> 1. **运行环境拓展**：从纯微信小程序拓展为**双端支持（微信小程序 + Web 网页端）**，满足更多场景使用。
> 2. **UI 渲染重构**：弃用传统 DOM 旋转拼接，采用现代 CSS3 `conic-gradient` (圆锥渐变) 绘制标准扇形大转盘。
> 3. **视觉体验升级**：引入高对比度独立配色、白色分割线、取消生硬黑描边并引入柔和文字阴影。
> 4. **隐藏模式强化**：将单次控结果升级为**“指定序列队列 + 自定义权重”**混合控制模式。

## 一、产品概述

本产品是一款面向家长的**儿童奖惩自定义转盘应用（支持微信小程序及 Web 端）**，核心用于日常家庭教育场景，通过转盘随机抽奖的趣味形式，对孩子日常优秀表现进行奖励、不良行为进行适度惩罚。支持家长自主创建、编辑、删除转盘及奖惩内容，同时内置强大的隐藏式人工控制系统（序列控制+权重控制），兼顾随机性与家长管控灵活性，适配不同年龄段孩子的家庭教育需求，操作简单、轻量化无复杂流程。

## 二、用户角色

核心用户：家长（使用者、管理者）

使用对象：儿童（体验者）

## 三、核心功能需求

### 1\. 统一的转盘主页（全新架构核心）

作为进入应用的默认首页，打破原有固定的“奖励/惩罚”双模式限制，采用全新的“主页列表 ➔ 游玩 ➔ 编辑”三层架构。

- **列表式主页**：展示所有已创建的完全自定义的专属转盘。
- **默认数据**：首次加载时自动生成“儿童日常行为奖励”和“儿童日常行为惩罚”两个基础模板（防删除保护）。
- **操作入口**：列表项提供直接进入游玩、编辑修改和删除（非默认模板）的快捷操作，底部提供全局的“+ 创建新转盘”按钮。

### 2\. 转盘游玩与自定义管理（新增/删除/编辑）

支持家长完全自主配置转盘内容，可按需创建多套转盘模板，适配不同场景、不同年龄孩子需求。

- **游玩页面**：点击主页列表里的任意转盘即可进入专属游玩页面。点击转盘中心按钮即可启动转动，转动结束后自动定格结果并展示。
- **新增转盘**：家长可自主创建全新转盘，自定义转盘名称、转盘分区数量（2-12项）、每个分区的文字内容、分区颜色（提供12种高饱和预设色彩供循环切换）。
- **编辑转盘**：对已有转盘的名称、分区内容、颜色、分区数量进行修改调整。
- **删除转盘**：支持删除任意自定义转盘，默认初始模板转盘可设置「禁止删除」，避免误操作清空。
- **内容自定义规则**：所有奖惩文案无固定限制，家长可自由输入（如奖励：多看20分钟绘本、免一次家务、获得小贴纸；惩罚：收拾玩具、背诵一首古诗、禁止看电视30分钟等）。

### 3\. 隐藏式人工控结果功能（核心特色 - V2升级版）

为满足家长灵活管控需求，内置**隐藏后台可控结果功能**，常态完全隐藏，儿童无法察觉，仅家长通过专属特殊操作唤醒。

- **隐藏机制**：常规界面无任何操控入口、无按钮、无文字提示，完全隐藏。
- **唤醒方式（特殊操作）**：设定专属手势唤醒（连续点击转盘中心5次，单次间隔≤500ms），唤醒后弹出管控面板。
- **【更新点】序列与权重混合控制系统**：
  1. **指定序列优先级（最高）**：家长可以按顺序多次点击选项，将其加入“结果序列队列”。孩子后续点击“开始”，转盘会严格按照家长设定的队列顺序依次出结果。
  2. **自定义权重抽取（次级）**：当序列队列为空（或消耗完毕）时，系统进入常态抽取。家长可设置常态为“纯随机”或“按权重”。在“按权重”模式下，家长可为每个选项单独设定权重值（数字），转盘将根据权重比例进行概率抽取。
  3. **纯随机兜底（最低）**：若未设置序列且常态为“纯随机”，或所有选项权重均设为 0，则自动回归完全平均概率的随机抽取模式。
- **无感体验**：外在动画、转动效果在任何控制模式下均与真实随机完全一致，无破绽。

### 4\. 转盘模板管理

- 默认自带基础奖励、惩罚模板各一套，供新手家长直接使用

- 所有自定义转盘自动保存至本地，支持随时切换、调用、编辑

- 支持设置常用转盘为默认首页，打开小程序直接展示常用转盘

## 四、交互与视觉需求

### 1\. 视觉风格

- **整体风格**：童趣、清新、柔和，色彩明亮不刺眼，界面简洁无多余广告、弹窗，适配儿童视觉体验。奖励转盘以暖色调为主，惩罚转盘以清爽冷色调区分。
- **【更新点】大转盘UI重构**：放弃悬浮卡片式拼接，采用纯 CSS3 `conic-gradient` 渲染真实的无缝扇形大转盘。每个选项分配独立的高对比度颜色，且板块间加入纯白实线分割，保证强视觉边界。
- **【更新点】文本呈现优化**：加大转盘文本字号（36rpx），取消黑色粗描边，改用双层柔和下拉阴影（Drop Shadow），确保白色文字在任何底色上均清晰易读且不显脏。

### 2\. 交互规则

- **双端适配**：核心代码同时支持在微信小程序和 Web PC/H5 环境中运行，Web 端加入 `max-width` 居中限制模拟手机壳体验。

- 转盘转动流畅，启停动画自然，结果定格有醒目提示（文字高亮、轻微震动、弹窗提示）

- 自定义编辑界面操作简单，支持一键添加分区、删除单条内容，无需复杂操作

- 隐藏功能唤醒无弹窗提示、无明显界面变化，仅家长知晓，隐蔽性拉满

- **硬性技术参数（开发必落地）**：
**转盘点击防抖**：防抖时长 **800ms**，防止用户连续多次点击触发多次转盘转动，防抖期间点击无效

- **隐藏功能唤醒点击间隔**：连续点击转盘中心5次，**单次点击间隔≤500ms** 判定为有效触发；若任意两次点击间隔超过500ms，计数自动清零，重新累计

- **隐藏功能触发冷却**：单次唤醒成功后，设置**2s冷却时间**，避免短时间重复触发唤醒逻辑

- **转盘动画时长**：默认转动总时长 **3000ms**，先加速1000ms、匀速1000ms、减速1000ms，定格效果统一，无快慢差异

## 五、非功能需求

### 1\. 性能需求

小程序启动速度快，转盘转动无卡顿、延迟，编辑保存内容实时生效，无加载等待。

### 2\. 体验需求

无广告、无付费弹窗、无跳转链接，纯工具属性，专注家庭教育场景；数据本地保存，卸载重装前保留所有自定义转盘数据。

### 3\. 隐私需求

无需授权多余隐私权限，仅基础微信授权，不收集用户、儿童个人信息，数据安全无泄露风险。

## 六、功能优先级

**P0（必做核心）**：统一的主页列表、转盘随机转动定格、自定义奖惩内容、新增/删除/编辑无限量转盘

**P1（核心特色）**：隐藏式人工控制转盘结果功能、专属手势唤醒机制

**P2（优化体验）**：默认模板、常用转盘置顶、界面视觉优化

> （注：文档部分内容可能由 AI 生成）
